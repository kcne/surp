/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * SURP API
 * Backend API for SURP, providing tenant-scoped authentication, operational health endpoints, and secure session management for transportation workflows. Protected endpoints require BOTH Authorization: Bearer <accessToken> and X-Tenant-Slug headers (except /platform/* routes), and the tenant must match the token claim.
 * OpenAPI spec version: 0.1.0
 */
import type { OrphanedReservationItemDtoReason } from './orphanedReservationItemDtoReason';

export interface OrphanedReservationItemDto {
  reservationId: string;
  passengerName: string;
  passengerPhone: string;
  travelDate: string;
  rideName: string;
  lineName: string;
  departureStationName: string;
  arrivalStationName: string;
  /** Departure time stored on the reservation. */
  currentDepartureTime: string;
  /**
   * Departure time of the single instance that can claim it, when there is one.
   * @nullable
   */
  targetDepartureTime: string | null;
  /** @nullable */
  targetArrivalTime: string | null;
  seatNumber: number;
  /**
   * Seat the reservation would take after repair. Differs from seatNumber when the original seat was sold again while this reservation was invisible; null when the target instance is full.
   * @nullable
   */
  targetSeatNumber: number | null;
  /** Which cause of unreachability this is. "No departure that day" has several, and an agency can only act once it knows which one. */
  reason: OrphanedReservationItemDtoReason;
  /** Short Serbian name for the reason, for a table cell. */
  reasonLabel: string;
  /** What the agency should do about this row, in Serbian. Usually the advice for the reason, but a row whose target departure has no free seat says so instead. */
  reasonAdvice: string;
  canRepair: boolean;
  /** Reservation stations that are no longer on the ride route, if any. */
  offRouteStationNames: string[];
}
