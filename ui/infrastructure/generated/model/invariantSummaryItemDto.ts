/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * SURP API
 * Backend API for SURP, providing tenant-scoped authentication, operational health endpoints, and secure session management for transportation workflows. Protected endpoints require BOTH Authorization: Bearer <accessToken> and X-Tenant-Slug headers (except /platform/* routes), and the tenant must match the token claim.
 * OpenAPI spec version: 0.1.0
 */
import type { InvariantSummaryItemDtoSeverity } from './invariantSummaryItemDtoSeverity';

export interface InvariantSummaryItemDto {
  key: string;
  title: string;
  description: string;
  /** What a person should do about what a repair cannot or would not fix. */
  manualAdvice: string;
  severity: InvariantSummaryItemDtoSeverity;
  /** Whether the last stored run covered this check at all. False for a check added since that run, which reports no count rather than a clean one. */
  checked: boolean;
  scannedCount: number;
  violationCount: number;
  repairableCount: number;
  hasRepair: boolean;
  /** Start of the unbroken run of checks in which this invariant has been failing. Absent when it is currently clean. This is the date a postmortem asks for. */
  failingSince?: string;
  /** True when the failing streak continues beyond the retained response window, so failingSince means at least since that instant. */
  failingSinceIsLowerBound: boolean;
}
