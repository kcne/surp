export type StorefrontSections = {
  hero: boolean;
  rides: boolean;
  about: boolean;
};

export const DEFAULT_STOREFRONT_SECTIONS: StorefrontSections = {
  hero: true,
  rides: true,
  about: true
};
